<% content_for(:page_title) do "Create Template" end %>

<div id="rate-show">
 <h2>Create Rate Table Template</h2>
  <div id='usage-template-container' data-url="<%= templates_path %>"
    data-loadurl="<%= templates_path %>">
    <%= render "attribs" %>
    <%= render "toolbar_new" %>
    <%= render "rates" %>
  </div>
</div>
